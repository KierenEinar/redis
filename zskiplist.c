//// Created by CF on 2021/6/9.//#include "zskiplist.h"#include "sds.h"#include <stdlib.h>#include <string.h>#define ZSKIPLIST_MAXLEVEL 32#define ZSKIPLIST_P 0.25typedef struct zskiplistNode {    sds ele;    double score;    struct zskiplistNode *backward;    struct zskiplistLevel {        struct zskiplistNode *forward;        unsigned long span;    } level[];}zskiplistNode;typedef struct zskiplist {    zskiplistNode *header, *tail;    unsigned int level;    unsigned long length;}zskiplist;// 创建一个空节点zskiplistNode * zslCreateNode(int level, sds ele, double score) {    zskiplistNode *zn = malloc(sizeof(*zn) + level*sizeof(struct zskiplistLevel));    zn->ele = ele;    zn->score = score;    return zn;}// 创建一个空列表zskiplist * zslCreate(void) {    zskiplist *zsl = malloc(sizeof(*zsl));    zsl->level = 1;    zsl->length = 0;    zsl->header = zslCreateNode(ZSKIPLIST_MAXLEVEL, NULL, 0);    for (int i=0;i<ZSKIPLIST_MAXLEVEL; i++) {        zsl->header->level[i].forward = NULL;        zsl->header->level[i].span = 0;    }    zsl->header->backward = NULL;    zsl->tail = NULL;    return zsl;}// 创建一个随机的levelint zslRandomLevel() {    int level = 1;    while (rand() & 0xffff >= ZSKIPLIST_P * 0xffff) {        level++;    }    return (level >= ZSKIPLIST_MAXLEVEL) ? ZSKIPLIST_MAXLEVEL : level;}// 插入一个节点zskiplistNode *zslInsert(zskiplist *zsl, double score, sds ele) {    zskiplistNode *update[ZSKIPLIST_MAXLEVEL], *x; // 每一层小于score的最大ele, x是当前的节点    unsigned int rank[ZSKIPLIST_MAXLEVEL];     // 每一层小于score的总span    x = zsl->header;    for(int i=zsl->level-1; i>=0;i--) {        rank[i] = (i == zsl->level-1 ? 0 : rank[i+1]);        while (x->level[i].forward &&                (x->level[i].forward->score < score ||                    (x->level[i].forward->score == score &&                        strcmp(x->level[i].forward->ele, ele) < 0))) {            rank[i] += x->level[i].span;            x = x->level[i].forward;        }        update[i] = x;    }}